<view class="page">
  <view class="notice-bar" bindtap="goNotice">
    <text class="notice-label">公告</text>
    <view class="notice-marquee">
      <view class="notice-track">
        <text class="notice-text">{{notice.marqueeText || notice.title}}</text>
      </view>
    </view>
  </view>

  <view class="video-hero" bindtap="goVideo">
    <image class="video-cover" src="{{video.coverUrl}}" mode="aspectFill"></image>
    <view class="video-shade"></view>
    <view class="video-top">
      <text class="tag">教学视频</text>
      <text class="video-time">{{video.duration}}</text>
    </view>
    <view class="play">▶</view>
    <text class="video-title">{{video.title}}</text>
    <text class="video-desc">了解题库、考试、错题和离线缓存的使用方式</text>
  </view>

  <view class="section">
    <view class="section-head">
      <text class="section-title">学习推广</text>
    </view>
    <view class="list">
      <view class="card promo-card" wx:for="{{promotions}}" wx:key="id" data-id="{{item.id}}" bindtap="goPromotion">
        <image wx:if="{{item.imageUrl}}" class="promo-image" src="{{item.imageUrl}}" mode="aspectFill"></image>
        <view class="row between">
          <text class="title">{{item.title}}</text>
          <text class="tag gray">{{item.tag}}</text>
        </view>
        <text class="desc">{{item.desc}}</text>
      </view>
    </view>
  </view>
</view>
